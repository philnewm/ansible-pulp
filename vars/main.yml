---

podman_packages:
  - podman
  - systemd-container
  - slirp4netns

pulp_venv_system_packages:
  redhat:
    - python3-packaging
  debian:
    - python3-venv
    - python3-packaging

# check: ansible_collections/pulp/squeezer/plugins/module_utils/pulp_glue.py
pulp_venv_python_packages:
  - "pulp-cli[pygments]>=0.29.2,<0.40"  # includes pulp-glue
  - "pulp-cli-deb>=0.3.0,<0.5" 

...
